"use client";

import { HeroNav } from "@/components/hero-nav";

/**
 * Practice top — floating nav only (hero photo frame removed).
 */

export function PracticeHeroSection() {
  return <HeroNav />;
}
